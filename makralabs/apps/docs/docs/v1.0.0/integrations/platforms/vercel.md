# Vercel

The v1 Vercel guide documents the legacy deployment pattern for Makra-backed applications.

## Setup

- Add API keys to environment variables.
- Keep Makra traffic on the server.
- Validate preview and production independently.
