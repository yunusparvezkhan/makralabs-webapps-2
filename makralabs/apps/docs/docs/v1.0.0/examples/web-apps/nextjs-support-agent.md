# Next.js Support Agent

This v1 example shows a support assistant pattern that keeps Makra orchestration on the server and UI rendering in the app layer.

## Good practices

- Redact user secrets from logs.
- Keep all Makra credentials server-side.
- Normalize output before rendering it.
