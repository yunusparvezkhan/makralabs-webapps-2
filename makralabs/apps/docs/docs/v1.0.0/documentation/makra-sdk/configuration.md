# Configuration

In v1, explicit configuration is even more important because fewer defaults are applied automatically.

## Recommended options

- Set a clear `baseUrl`.
- Define timeouts explicitly.
- Add retries only for transient failures.

## Operational guidance

Keep local, staging, and production credentials separate and document any environment-specific overrides in your deployment runbook.
