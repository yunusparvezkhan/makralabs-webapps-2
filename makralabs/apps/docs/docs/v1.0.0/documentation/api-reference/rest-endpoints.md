# REST Endpoints

The v1 REST surface includes responses, sessions, and integration endpoints used by older Makra deployments.

## Common resources

- `POST /v1/responses`
- `POST /v1/sessions`
- `POST /v1/webhooks`

## Guidance

Prefer stable request payloads and version-pinned clients when maintaining long-lived integrations.
