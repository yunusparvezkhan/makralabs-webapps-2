# Next.js

Next.js remains a solid integration target in v1 when you want a clean separation between server-side orchestration and client rendering.

## Recommended pattern

Use route handlers or server utilities for all Makra interactions so secrets stay protected.
