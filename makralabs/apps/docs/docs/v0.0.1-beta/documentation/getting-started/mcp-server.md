# MCP Server

The v1 server guide captures the older contract shape while keeping the same discoverable path as v2.

## Overview

Use version-specific pages to call out older defaults, ports, or dependency expectations.

## Migration note

Link forward to the v2 page whenever the newer behavior is the preferred path.
