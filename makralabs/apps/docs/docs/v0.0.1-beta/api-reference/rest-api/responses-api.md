# Responses API

The v1 Responses API supports one-shot generation and powers many existing Makra production integrations.

## Endpoint

```http
POST /v1/responses
```

## Guidance

Pin your client version and verify request payload compatibility before rolling changes into long-lived systems.
