# Local development

Makra works great in local development when your docs, code, and prompts change quickly.

## Environment variables

At minimum:

`MAKRA_API_KEY` and `MAKRA_WORKSPACE`.

## Suggested workflow

- Keep a single “playground” session id for fast iteration
- Use labels to separate experiments from real traces
- Record retrieval bundles so you can compare changes
