# Installation

The v1 SDK installation page captures the older package and runtime expectations in a dedicated onboarding step.

## Overview

Keep install commands, runtime support, and environment prerequisites together so setup is friction-free.
