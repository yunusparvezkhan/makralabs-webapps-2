# Authentication

Authentication deserves its own page so developers can find token setup without digging through a quickstart.

## Overview

Document how to create credentials, where to store them, and how to rotate them safely.

## Include these details

- Required environment variable names.
- Local development fallback behavior.
- The first request users can make to confirm auth is valid.
