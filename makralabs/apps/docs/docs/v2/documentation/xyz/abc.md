# ABC

Use this placeholder page to prove the docs router supports deeper paths such as `/v2/documentation/xyz/abc`.

## Overview

The content is intentionally simple, but the URL shape mirrors the real hierarchy you asked for.

## Recommended use

- Keep experimental material in its own subsection.
- Promote it into a named product area once the content stabilizes.

## Editorial note

If this section becomes permanent, rename `xyz` and `abc` to product-specific terms.
